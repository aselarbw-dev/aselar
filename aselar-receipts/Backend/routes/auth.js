
/*
const express=require("express")
const {registerBusiness,loginBusiness,logoutBusiness,getUserProfile}=require("../controllers/auth.js")
const multer = require('multer');
const upload = multer({ dest: 'uploads/' });  
//const {protect}=require("../middlewares/protect.js")
const router = express.Router();
router.post("/business-signup",upload.single('profilePicture'),registerBusiness)
router.post("/business-login",loginBusiness)
router.get("/logout",logoutBusiness)
router.get("/get-profiles",getUserProfile)
module.exports=router
*/